const UserPosts = () => {
    const getData = async () => {
        try {
            
            // const result = await fetch('http://localhost:5000/getUserPosts/' + {post.id})
        } catch (error) {
            
        }
    }


    return ( 
        <div className="UserPosts">
            User Posts
        </div>
     );
}
 
export default UserPosts;